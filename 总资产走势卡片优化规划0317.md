# 总资产走势卡片优化规划

> 创建日期：2026-03-17
> 状态：规划中（待实施）

## 一、问题描述

### 当前设计矛盾
总资产走势卡片中的"区间涨跌"指标计算方式为：

```typescript
changeRate = (期末总市值 - 期初总市值) / 期初总市值 × 100%
```

**问题**：该指标受资金进出影响大，不能真实反映投资能力。

### 案例说明

假设某组合在30天内的变化：

| 时间 | 操作 | 总市值 | 总成本 | 说明 |
|------|------|--------|--------|------|
| 1月1日 | - | 10万 | 10万 | 期初 |
| 1月10日 | 买入10万 | 20万 | 20万 | 新增资金 |
| 1月20日 | 买入10万 | 30万 | 30万 | 新增资金 |
| 1月31日 | - | 33万 | 30万 | 期末（实际涨10%）|

**当前"区间涨跌"显示**：
```
(33万 - 10万) / 10万 = 230%
```

**实际投资收益率**：
```
(33万 - 30万) / 30万 = 10%
```

**问题**：230% vs 10%，差异巨大，容易误导用户。

---

## 二、与收益率走势的对比

| 图表 | 指标 | 计算基准 | 是否反映真实收益 | 适用场景 |
|------|------|----------|------------------|----------|
| **总资产走势** | 区间涨跌 | 总市值变化 | ❌ 受资金进出影响大 | 看财富规模变化 |
| **收益率走势** | 收益率 | (市值-成本)/成本 | ✅ 反映真实投资能力 | 看投资表现 |

**核心矛盾**：总资产走势的"区间涨跌"与收益率走势的"收益率"口径不一致，用户可能困惑。

---

## 三、改进方案

### 方案1：替换"区间涨跌"为"期间收益率"（推荐）

**修改内容**：
- 将"区间涨跌"改为"期间收益率"
- 使用后端已计算的 `return_rate` 或 `twr`

**代码位置**：`frontend/src/components/portfolio/PortfolioValueChart.tsx`

```typescript
// 修改前
changeRate: ((last.total_value - first.total_value) / first.total_value) * 100

// 修改后（使用简单收益率）
changeRate: last.return_rate

// 或使用TWR（更专业）
changeRate: last.twr
```

**优点**：
- 与收益率走势图表口径一致
- 真实反映投资表现，不受资金进出影响
- 用户可以在总资产走势中直接看到收益率变化

**缺点**：
- 无法反映资产规模的绝对增长（但这不是本图表的重点）

---

### 方案2：保留当前计算但明确标注

**修改内容**：
- 指标名称改为"资产规模变化"
- 添加提示说明资金进出的影响

```typescript
<Statistic
  title="资产规模变化"  // 原名"区间涨跌"
  value={stats.changeRate}
  suffix="%"
  description="含新增/取出资金影响"  // 添加说明
/>
```

**优点**：
- 不改计算逻辑，风险低
- 用户明确知道此指标包含资金进出

**缺点**：
- 仍可能造成困惑
- 两个图表的"涨跌"含义不一致

---

### 方案3：展示双指标（信息最完整）

**修改内容**：
在统计区域展示4个指标：

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│  当前资产    │  当前收益    │ 期间收益率   │  资产规模变化 │
│  ¥330,000   │  +¥30,000   │   +10.0%    │   +230%     │
│             │  (真实收益)  │ (推荐关注)   │ (含资金进出)  │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

**代码修改**：
```typescript
<Space size="large" style={{ marginBottom: 16, flexWrap: 'wrap' }}>
  <Statistic title="当前资产" value={stats.current} prefix="¥" precision={2} />
  <Statistic title="当前收益" value={stats.currentProfit} prefix="+" precision={2} />
  <Statistic
    title="期间收益率"
    value={last.return_rate}
    suffix="%"
    valueStyle={{ color: last.return_rate >= 0 ? '#cf1322' : '#3f8600' }}
  />
  <Statistic
    title="资产规模变化"
    value={stats.changeRate}
    suffix="%"
    description="含新增资金"
  />
</Space>
```

**优点**：
- 信息最完整，用户可自主选择关注指标
- 满足不同用户的使用场景

**缺点**：
- 空间占用较大，可能需要调整布局

---

## 四、推荐实施方案

**综合评估**：推荐**方案3（双指标展示）**，但简化为3个核心指标：

```
当前资产        期间收益率（重点）      当前收益
 ¥330,000         +10.0%               +¥30,000
                                红色=盈利 绿色=亏损
```

**理由**：
1. "期间收益率"是最核心的投资能力指标，应该突出展示
2. "当前资产"和"当前收益"提供绝对数值参考
3. 去掉容易产生歧义的"资产规模变化"，避免混淆

---

## 五、相关代码位置

| 文件 | 路径 | 相关行号 |
|------|------|----------|
| 总资产走势图表 | `frontend/src/components/portfolio/PortfolioValueChart.tsx` | 234-255（统计计算）、297-321（统计展示） |
| 收益率走势图表 | `frontend/src/components/portfolio/PortfolioReturnChart.tsx` | 参考对比 |
| 后端数据接口 | `backend/app/services/portfolio_service.py` | 967-978（返回数据结构） |

---

## 六、待决策事项

1. **指标选择**：使用 `return_rate`（简单收益率）还是 `twr`（时间加权收益率）？
   - 建议：默认 `return_rate`，专业用户可切换 `twr`

2. **布局方式**：3个指标还是4个指标？
   - 建议：3个指标，保持简洁

3. **颜色规范**：
   - 当前：红色=涨/盈利，绿色=跌/亏损（A股习惯）
   - 是否需要与国际惯例统一（绿色=涨，红色=跌）？

---

## 七、后续行动计划

- [ ] 确认设计方案（方案1/2/3）
- [ ] 确认指标类型（return_rate / twr / xirr）
- [ ] 实施代码修改
- [ ] 测试验证
- [ ] 更新使用说明文档
