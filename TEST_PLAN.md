# FundEval 修复测试计划

## 修复内容总结

### P0 级别修复（核心问题）
1. **双倍持仓问题** - 添加基金时设置初始份额为0，仅通过交易记录增加持仓
2. **卖出校验逻辑** - 移除重复计算 holding.shares 的代码

### P1 级别修复（用户体验）
3. **持仓明细刷新按钮** - 在 FundDetailDrawer 添加刷新按钮
4. **日期选择后自动获取净值** - 选择日期后自动获取该日期的历史净值

### P2 级别修复（边界防护）
5. **删除交易保护** - 删除买入交易时检查是否会导致负持仓
6. **交易日验证提示** - 选择非交易日时显示警告提示

---

## 测试场景

### 场景1：双倍持仓修复测试

**步骤**：
1. 进入组合管理页面
2. 选择一个组合，点击"添加基金"
3. 搜索并选择一只基金（如 000001）
4. 输入份额 100，金额自动计算
5. 点击"添加"

**预期结果**：
- 基金添加成功
- 持仓份额显示为 100（而非 200）
- 交易记录中显示一条买入记录

---

### 场景2：卖出校验逻辑测试

**步骤**：
1. 确保某基金持仓为 100 份
2. 点击该基金"卖出"
3. 尝试输入卖出份额 150
4. 点击"确认卖出"

**预期结果**：
- 系统拒绝卖出
- 提示错误信息："Insufficient shares for sale. Available: 100, Requested: 150"

---

### 场景3：持仓明细刷新按钮测试

**步骤**：
1. 打开某基金持仓明细抽屉
2. 点击"买入"，完成一笔新交易
3. 点击抽屉右上角的刷新按钮
4. 观察交易记录列表

**预期结果**：
- 刷新按钮点击后，交易记录重新加载
- 新交易出现在列表中
- 持仓摘要信息更新

---

### 场景4：日期选择后自动获取净值测试

**步骤**：
1. 点击"添加基金"
2. 选择一只基金
3. 修改日期为前几天（如5天前）
4. 观察"确认净值"字段

**预期结果**：
- 日期选择后，系统自动获取该日期的历史净值
- 净值字段自动填充为该日期的净值

---

### 场景5：非交易日警告测试

**步骤**：
1. 点击"添加基金"
2. 选择一只基金
3. 修改日期为周末或节假日（如上周六）
4. 观察提示信息

**预期结果**：
- 显示黄色警告提示："YYYY-MM-DD 可能不是交易日，未找到净值数据。请手动输入净值或选择其他日期。"

---

### 场景6：删除交易保护测试

**步骤**：
1. 确保某基金有以下交易记录：
   - 买入 100 份
   - 卖出 50 份（当前持仓 50 份）
2. 在交易记录中找到买入 100 份的记录
3. 点击删除

**预期结果**：
- 系统阻止删除
- 提示错误信息："Cannot delete this buy transaction: it would result in negative shares..."

---

## 自动化测试命令

### 启动服务
```bash
startup_tabs.bat
```

### 前端测试
```bash
cd frontend
npm run test
```

### 后端测试
```bash
cd backend
pytest tests/ -v
```

---

## 预期修复效果

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| 双倍持仓 | 买入100份显示200份 | 买入100份显示100份 |
| 卖出校验 | 可能超卖 | 正确校验，超卖被拒绝 |
| 刷新按钮 | 无 | 有，点击可刷新 |
| 日期净值 | 仅填充最新净值 | 根据日期获取历史净值 |
| 删除交易 | 可能导致负持仓 | 阻止会导致负持仓的删除 |
| 交易日验证 | 无提示 | 非交易日显示警告 |
