# FOF基金管理人视角的图表逻辑优化计划

## 当前问题诊断

### 问题1：收益率计算逻辑错误 ⚠️ 关键

**当前实现** (`backend/app/services/portfolio_service.py:849-879`):
```python
# 计算的是基于NAV变化的时间加权收益
daily_return_rate += weight * fund_return  # fund_return是基金当日涨跌幅
cumulative_return = ((1 + prev_cumulative / 100) * (1 + daily_return_rate / 100) - 1) * 100
```

**问题分析**:
- 当前计算的是**基金持仓的加权平均涨跌幅**
- 这不是FOF管理人要的"收益率"！
- FOF管理人要的是：**总盈亏金额 / 总成本** 或者 **(当前总市值 - 总成本) / 总成本**

**正确逻辑**:
```
当日收益率 = (当日总市值 - 当日总成本) / 当日总成本 × 100%
累计收益率 = (当前总市值 - 当前总成本) / 当前总成本 × 100%
```

---

### 问题2：缺少关键指标 ⚠️ 关键

**FOF管理人需要的指标**:

| 指标 | 当前状态 | 说明 |
|------|----------|------|
| 总资产规模走势 | ✅ 有 | 总市值曲线 |
| 总成本走势 | ✅ 有 | 总成本曲线 |
| **总收益金额** | ❌ 缺失 | 总市值 - 总成本，需要叠加在总资产图 |
| **当日收益额** | ❌ 缺失 | 当日市值变化 - 当日新增投入，需要柱状图 |
| 单位净值 | ❌ 缺失 | FOF的单位净值 = 总市值 / 总份额 |
| 单位净值变化 | ❌ 缺失 | 单位净值涨跌幅 |
| 区间涨跌幅 | ✅ 有 | 但计算逻辑需修正 |
| 最大回撤 | ❌ 缺失 | 区间内最大跌幅 |

---

### 问题3：性能严重问题 ⚠️ 严重

**当前实现分析** (`get_portfolio_history`):
```python
# 对每个日期（如30天）
for date in date_range:
    # 对每个持仓基金
    for holding in holdings:
        # 调用一次数据库或Wind API获取NAV
        nav_data = await cls._get_nav_for_date(db, fund_code, date)
```

**性能瓶颈**:
- 30天 × 10只基金 = 300次数据库/API调用
- 每次切换周期都重新计算
- 历史净值没有缓存
- FOF收益率没有缓存

**优化方案**:
1. **基金净值缓存**: 建立 `fund_nav_cache` 表，缓存每只基金的历史净值
2. **FOF收益曲线缓存**: 建立 `portfolio_return_cache` 表，缓存计算好的每日数据
3. **增量更新**: 只计算缺失的日期，不重新计算全部

---

### 问题4：周末/节假日净值缺失处理 ⚠️ 边界情况

**当前问题**:
```python
# _get_nav_for_date 方法
if nav_history and len(nav_history) > 0:
    return nav_history[0].nav
# 如果没有数据，返回None
return None
```

**风险**:
- 周末没有净值，曲线会出现断点
- 影响收益率计算的连续性
- 当日收益额无法计算

**解决方案**:
1. 向前查找最近交易日的净值
2. 标记当日为"估算"或"沿用昨日"
3. 当日收益额在周末显示为0或沿用基金估值

---

## 改进方案

### 方案1：修正收益率计算逻辑

**修改位置**: `backend/app/services/portfolio_service.py:840-886`

**新计算逻辑**:
```python
# 简单收益率（_money-weighted return_）
daily_return_rate = ((total_value - total_cost) / total_cost * 100) if total_cost > 0 else 0

# 累计收益率就是最后一天的daily_return_rate
# 不需要复杂的复合计算
```

---

### 方案2：新增关键指标计算

**扩展API返回数据结构**:
```python
class PortfolioHistoryData(BaseModel):
    date: str
    total_value: float           # 总市值
    total_cost: float            # 总成本
    total_profit: float          # 总收益（新增）
    daily_profit: float          # 当日收益额（新增）
    return_rate: float           # 收益率
    is_estimated: bool           # 是否估算（新增）
```

---

### 方案3：前端图表增强

#### 收益率走势卡片 (`PortfolioReturnChart.tsx`)

**新增**: 当日收益额柱状图（双Y轴）
```typescript
// 右轴：当日收益额
{
  name: '当日收益额',
  type: 'bar',
  yAxisIndex: 1,  // 使用右轴
  data: dailyProfits,
  itemStyle: {
    color: (params: any) => params.value >= 0 ? '#ff4d4f' : '#52c41a'
  }
}
```

**标注**: 估算净值用特殊标记

#### 总资产走势卡片 (`PortfolioValueChart.tsx`)

**新增**: 总收益线（双Y轴）
```typescript
// 右轴：总收益
{
  name: '总收益',
  type: 'line',
  yAxisIndex: 1,
  data: totalProfits,
  lineStyle: { color: '#faad14' }
}
```

---

### 方案4：后端缓存机制

#### 4.1 基金净值缓存表

```sql
CREATE TABLE fund_nav_cache (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    fund_code VARCHAR(20) NOT NULL,
    date DATE NOT NULL,
    nav DECIMAL(10, 4),
    is_estimated BOOLEAN DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(fund_code, date)
);
```

#### 4.2 FOF收益曲线缓存表

```sql
CREATE TABLE portfolio_return_cache (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    portfolio_id VARCHAR(100) NOT NULL,
    date DATE NOT NULL,
    total_value DECIMAL(15, 2),
    total_cost DECIMAL(15, 2),
    total_profit DECIMAL(15, 2),
    daily_profit DECIMAL(15, 2),
    return_rate DECIMAL(10, 4),
    is_estimated BOOLEAN DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(portfolio_id, date)
);
```

#### 4.3 缓存更新策略

```python
# 获取历史数据时
async def get_portfolio_history_with_cache(portfolio_id, period):
    # 1. 检查缓存中是否有数据
    cached_data = await get_cached_data(portfolio_id, start_date, end_date)

    # 2. 找出缺失的日期
    missing_dates = find_missing_dates(cached_data, start_date, end_date)

    # 3. 只计算缺失的日期
    new_data = await calculate_for_dates(portfolio_id, missing_dates)

    # 4. 保存到缓存
    await save_to_cache(portfolio_id, new_data)

    # 5. 合并返回
    return merge_and_sort(cached_data, new_data)
```

#### 4.4 缓存失效机制

- **交易录入**: 当用户录入新的交易时，从新交易日期开始清空缓存
- **每日更新**: 每天收盘后更新当日数据
- **手动刷新**: 提供刷新按钮强制重新计算

---

### 方案5：周末/节假日处理

```python
async def get_nav_for_date_safe(db, fund_code, date):
    """获取某日期净值，如果当天无数据则向前查找最近交易日"""
    # 1. 尝试获取当天净值
    nav = await get_nav(db, fund_code, date)
    if nav:
        return {'nav': nav, 'is_estimated': False}

    # 2. 向前查找最近交易日（最多找10天）
    for i in range(1, 10):
        prev_date = date - timedelta(days=i)
        nav = await get_nav(db, fund_code, prev_date)
        if nav:
            return {'nav': nav, 'is_estimated': True, 'actual_date': prev_date}

    # 3. 都找不到返回None
    return None
```

---

## 实施优先级

| 优先级 | 任务 | 原因 |
|--------|------|------|
| P0 | 修正收益率计算逻辑 | 核心指标错误，必须优先修复 |
| P0 | 新增总收益和当日收益额 | FOF管理人核心需求 |
| P1 | 前端图表双Y轴展示 | 依赖后端数据结构调整 |
| P1 | 净值缓存机制 | 性能优化，数据量大时必须 |
| P2 | FOF收益曲线缓存 | 进一步提升性能 |
| P2 | 周末/节假日处理 | 边界情况完善 |
| P3 | 最大回撤计算 | 额外指标增强 |

---

## 文件修改清单

### 后端文件
1. `backend/app/models/portfolio.py` - 扩展历史数据结构
2. `backend/app/services/portfolio_service.py` - 修正计算逻辑，新增缓存
3. `backend/app/db_models/` - 新增缓存表模型
4. `backend/app/routers/portfolios.py` - 新增缓存刷新API

### 前端文件
5. `frontend/src/components/portfolio/PortfolioReturnChart.tsx` - 新增当日收益额柱状图
6. `frontend/src/components/portfolio/PortfolioValueChart.tsx` - 新增总收益线
7. `frontend/src/types/index.ts` - 扩展类型定义

---

## 验证步骤

1. 选择一个有历史数据的组合
2. 对比修正前后的收益率数值
3. 验证总资产图上的总收益线正确性
4. 验证收益率图上的当日收益额柱状图
5. 测试切换周期时的加载速度
6. 测试录入新交易后缓存是否正确更新
7. 测试周末日期的数据显示

---

## 待确认问题

### 问题1：收益率计算口径 ✅ 已确认

**决策**：三种收益率全部实现

| 收益率类型 | 用途 | 显示位置 |
|------------|------|----------|
| **简单收益率** | 主指标，反映实际盈亏 | 卡片顶部统计区 |
| **时间加权收益率(TWR)** | 评估投资能力 | 详情tooltip或次要位置 |
| **资金加权收益率(XIRR)** | 年化对比 | 详情tooltip或次要位置 |

**页面底部说明文案**：
```
收益率计算说明：
• 简单收益率 = (总市值 - 总成本) / 总成本 × 100%
  反映实际盈亏比例，受资金流入流出影响
• 时间加权收益率(TWR) = ∏(1 + 区间收益率) - 1
  剔除资金进出影响，反映投资能力
• 资金加权收益率(XIRR) = 考虑时间价值的年化收益率
  可与理财产品收益率横向对比
```

### 问题2：当日收益额的计算口径 ✅ 已确认

**公式**：
```
当日收益额 = 当日总市值 - 昨日总市值 - 当日新增投入 + 当日赎回
```

**说明**：
- 如果是今天买入的，当日收益额不包含这部分买入带来的市值增加
- 如果是今天卖出的，当日收益额不包含这部分卖出导致的市值减少
- 基金分红暂不考虑（后续可作为增强功能）

### 问题3：缓存粒度 ✅ 已确认

**决策**：全系统共享基金净值缓存

**理由**：
- 同一只基金（如000001）可能在多个组合中出现
- 共享缓存避免重复存储，节省空间
- 所有组合使用相同的净值数据，保持一致性

**缓存表设计**：
```sql
-- fund_nav_cache: 全系统共享
fund_code | date | nav | is_estimated | updated_at

-- portfolio_return_cache: 每个组合单独缓存
portfolio_id | date | total_value | total_cost | return_rate | ...
```

### 问题4：估算数据标注 ✅ 已确认

**决策**：需要标注

**标注方式**：
1. **当日收益额柱状图**：
   - 使用估算数据时，颜色较浅（半透明）
   - Tooltip显示"估算值"字样

2. **收益率曲线**：
   - 使用估算数据时，线条样式改为虚线或点线
   - Tooltip显示"基于估算净值计算"

3. **总资产线**：
   - 在tooltip中标注估算状态

**后端标记**：
```python
{
    'date': '2026-03-13',
    'total_value': 1650000,
    'is_estimated': True,  # 标记是否使用了估算净值
    'actual_nav_date': '2026-03-12'  # 实际使用的净值日期
}
```

---

## 实施状态

### Phase 1：核心逻辑修正（P0）✅ 已完成
- [x] 修正收益率计算逻辑（简单收益率）
- [x] 新增总收益和当日收益额计算
- [x] 新增TWR和XIRR计算

### Phase 2：前端展示（P0）✅ 已完成
- [x] 收益率卡片：双Y轴（收益率线 + 当日收益额柱状图）
- [x] 总资产卡片：双Y轴（总市值/总成本线 + 总收益线）
- [x] 添加计算说明footer

### Phase 3：性能优化（P1）✅ 已完成
- [x] 基金净值缓存表
- [x] FOF收益曲线缓存表
- [x] 缓存失效逻辑（交易录入/删除时）

### Phase 4：边界处理（P2）✅ 已完成
- [x] 周末/节假日净值处理
- [x] 估算数据标注

---

## 确认清单

- [x] 收益率计算口径：三种全部实现
- [x] 当日收益额公式确认
- [x] 缓存策略：全系统共享基金净值
- [x] 估算数据标注：需要标注

**状态**：✅ 所有实施已完成

---

## 待执行操作

1. 执行数据库表创建脚本：
   ```bash
   cd backend
   sqlite3 fund_valuation.db < create_cache_tables.sql
   ```

2. 重启后端服务以加载新代码

3. 验证图表功能正常
